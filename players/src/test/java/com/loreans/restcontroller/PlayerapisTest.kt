/*
package com.loreans.restcontroller

import com.loreans.PlayersDomainAppStarter
import org.jetbrains.spek.api.Spek
import org.jetbrains.spek.api.dsl.describe
import org.jetbrains.spek.api.dsl.given
import org.jetbrains.spek.api.dsl.it
import org.junit.jupiter.api.Assertions.*
import org.junit.runner.RunWith
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.test.context.junit4.SpringRunner
import org.springframework.test.context.web.WebAppConfiguration
import org.springframework.test.web.servlet.MockMvc
import org.springframework.web.context.WebApplicationContext

@RunWith(SpringRunner::class)
@SpringBootTest(classes = arrayOf(PlayersDomainAppStarter::class))
@WebAppConfiguration
internal class PlayerapisTest : Spek({
    given("mock mvc") {
        @Autowired
        var webApplicationContext: WebApplicationContext;

        beforeEachTest {
            val mockMvc: MockMvc
        }

        it(" register player api should give a successful response") {


        }
    }
})*/
